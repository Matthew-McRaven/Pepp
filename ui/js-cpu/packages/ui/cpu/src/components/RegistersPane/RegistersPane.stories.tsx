import React from 'react';
import RegistersPane from './RegistersPane';

export default {
  title: 'RegistersPane',
  component: RegistersPane,
  argTypes: {
  },
};

const Template = () => <RegistersPane />;

export const withTemplate = Template.bind({});

import { TypedNode, SectionGroup } from '../ast/nodes';

export 
export const joinSectionGroups = (tree: TypedNode) => {
    const groups = new Map<string, SectionGroup>();
  tree.walk((node:TypedNode) => {
    if()
  });
};

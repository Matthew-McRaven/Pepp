This is a new test directory in which each subdirectory can be compiled separately.
Moving forward, all of the unit tests will be a single target and each integration test will be a separate target.

For now, this directory only holds integration tests
